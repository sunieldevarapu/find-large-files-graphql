name: Find Large Files (Recursive GraphQL)

on:
  # push:
  #   branches: [ main ]
  # pull_request:
  #   branches: [ main ]
  workflow_dispatch:

jobs:
  scan-large-files:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: pip install requests

    - name: Run large file scanner (Recursive GraphQL)
      env:
        GITHUB_TOKEN: ${{ secrets.REPO_TOKEN1 }}
      #run: python find-large-files.py

    - name: Debug token (test only)
      run: |
        echo "Token length: ${#GITHUB_TOKEN}"
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

